{
  "version": 2,
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "frontend/dist",
  "env": {
    "NODE_VERSION": "18"
  },
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
